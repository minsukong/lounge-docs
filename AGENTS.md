# Project guidance

## 1. 기준 문서와 우선순위

- 가이드를 작성하기 전에 `README.md`, `init/index.html`, `monorepo/index.html`, `frontend_guide/index.html`과 대상 가이드의 기존 파일을 확인합니다.
- 애플리케이션 저장소 구조, 공통 패키지, 계약, 의존성, CI/CD와 배포 단위는 `monorepo/index.html`을 현재 기준으로 사용합니다.
- `monorepo/index.html`의 폴더 구조는 확정안이 아니므로 새 가이드에서 임의로 폴더나 패키지를 확정하지 않습니다.
- `app/app.html`과 `frontend_guide/index.html`은 영역별 상세 문서입니다. 내용이 충돌하면 `monorepo/index.html`을 우선합니다.
- 기존 기준과 다른 내용이 필요하면 상세 가이드에서 조용히 변경하지 않고 충돌 지점과 변경 필요성을 먼저 알립니다.

## 2. 가이드 작성 순서

- 사용자가 내용 정리나 논의만 요청하면 파일을 수정하지 않습니다.
- 먼저 문서의 목적, 적용 범위와 목차를 대화에서 정리합니다.
- 사용자가 초안 작성을 요청하면 대상 폴더의 `draft.md`에 내용을 작성하거나 갱신합니다.
- 사용자가 HTML 적용을 요청한 이후에만 대상 가이드의 `index.html`을 수정합니다.
- 새 가이드의 최종 경로가 확정되면 `README.md`의 링크와 실제 경로가 일치하는지 확인합니다.

## 3. 내용 작성 원칙

- 가이드는 백과사전식 설명보다 개발과 리뷰에 필요한 판단 기준을 제공합니다.
- 긴 설명보다 짧은 원칙과 실제 코드 예시를 사용합니다.
- 하나의 문단에는 하나의 주제만 작성합니다.
- 아직 확정되지 않은 기술, 폴더, API와 운영 규칙을 확정된 기준처럼 작성하지 않습니다.
- 아직 정해지지 않은 내용은 `TBD` 또는 명확한 상위 제목과 안내 문장으로 확정된 기준과 구분합니다.
- `추후 확정 항목`, `미확정 항목`, `우선 결정 항목`처럼 상위 제목이나 안내 문장이 범위를 명확히 알리면 하위 목록마다 `TBD`를 반복하지 않습니다.
- 문맥만으로 미확정 여부를 알기 어려운 개별 내용에는 `TBD`를 사용하고, 무엇이 미확정인지와 어떤 결정이 필요한지를 짧게 함께 작성합니다.
- 확정된 내용과 `TBD`를 같은 문장이나 목록 항목에 섞지 않고 별도 문단 또는 항목으로 분리합니다.
- 코드 블록이나 문맥만으로 예시임을 알 수 있으면 `EXAMPLE`, `예시입니다` 같은 라벨과 설명을 반복하지 않습니다.
- 내용이 확정되면 `TBD` 표시를 제거하고 관련 기준 문서와 상세 가이드를 함께 확인합니다.
- 실제 사용처가 없는 폴더, 공통 타입, Wrapper, Adapter와 추상화 구조를 미리 만들도록 안내하지 않습니다.
- 기능 가까이에서 작게 시작하고, 실제 반복과 동일한 변경 이유가 확인될 때 공통화하는 원칙을 유지합니다.
- 예시 코드는 현재 `frontend_guide/index.html`의 기술 스택과 구조에 모순되지 않아야 합니다.
- 권장 코드와 피해야 할 코드를 함께 보여주되, 같은 내용을 반복해서 설명하지 않습니다.
- 주제의 범위를 벗어나는 상세 설계는 현재 문서에서 제외하고, 결정이 필요하면 `TBD`로만 표시합니다.
- 향후 문서 구조가 확정되지 않은 상태에서 `문서 확장 원칙`, 후속 가이드 후보 또는 문서 분리 계획을 미리 작성하지 않습니다.
- 관련 문서 링크만 모은 독립 섹션은 사용자가 요청한 경우에만 작성합니다. 현재 기준을 이해하는 데 직접 필요한 기존 문서는 해당 내용 가까이에서만 연결합니다.

## 4. HTML 작성 규칙

- HTML 가이드는 반드시 `init/index.html`을 템플릿으로 사용합니다.
- 템플릿의 `header`, `main`, `section`, `hr`, `footer` 구조와 공통 자산 연결 방식을 유지합니다.
- 주요 영역 제목은 번호가 포함된 `h2`로 작성하고 필요한 경우 `bx-active` 클래스를 사용합니다.
- 공통 `common.js`가 `h2`를 기준으로 우측 메뉴를 생성하므로 별도의 목차 메뉴를 직접 만들지 않습니다.
- Header에는 문서 종류, 제목과 설명만 작성하고 Footer에는 문서명만 작성합니다.
- 수기로 관리하는 가이드에는 문서 버전, 작성일, 수정일, 개정일과 `Draft` 같은 상태 정보를 표시하지 않습니다.
- 특정 시점에만 적용되는 문서가 아니라면 제목과 설명에 `초기`, `도입 단계`, `시작 단계` 같은 수명 주기 표현을 사용하지 않습니다. 상태 초기화처럼 기술적 의미가 있는 용어는 유지합니다.
- 내부 문서는 상대 경로로 연결하고 링크 텍스트만으로 목적을 이해할 수 있게 작성합니다.
- 코드 예시는 `<pre class="hljs"><code data-language="...">` 구조를 사용합니다.
- `data-language`는 언어 이름만 표시하므로 이것만 추가하고 구문 강조가 적용됐다고 판단하지 않습니다.
- `text`를 제외한 TypeScript, TSX, JavaScript, JSON 등의 코드에는 Highlight.js 호환 `hljs-*` 토큰 마크업을 생성하여 구문 강조를 적용합니다.
- 구문 강조를 적용해도 사용자가 읽는 코드의 문자, 공백과 줄바꿈은 원본 예시와 같아야 합니다.
- 코드 안의 `<`, `>`, `&`는 HTML Entity로 변환하고, 구문 강조용 `<span class="hljs-*">`만 실제 태그로 사용합니다.
- HTML에서 개별 미확정 내용을 표시할 때는 일반 코드와 구분할 수 있도록 `<code class="badge-tbd">TBD</code>`를 사용하고, 결정이 필요한 내용을 같은 문단에 작성합니다. 상위 제목이나 안내 문장이 이미 미확정 범위를 명확히 알리면 반복 표시는 생략합니다.
- `blockquote` 안에서 `<code class="badge-tbd">TBD</code>`를 사용할 때는 Badge 뒤에 콜론(`:`)을 붙이지 않고 설명을 바로 이어서 작성합니다.
- 작성 예시: `<code class="badge-tbd">TBD</code>: 폼 검증 도구와 적용 범위는 프로젝트 구성 확정 후 결정합니다.`
- `blockquote`는 반드시 강조해야 하는 핵심 원칙, 경고, 문서 우선순위 또는 실제 인용에만 사용합니다.
- 코드 예시의 소개, 일반 설명, 문서 상태, 참고 링크와 단순 `TBD` 안내는 기본적으로 일반 문단을 사용합니다.
- `blockquote`의 시각적 강조가 없어도 의미와 중요도가 유지되는 내용은 `blockquote`로 감싸지 않습니다.
- 표를 사용할 때는 Header와 `scope`를 제공하고, 단순 목록으로 충분한 내용은 표로 만들지 않습니다.
- 다이어그램은 관계나 흐름을 설명하는 데 실제 도움이 될 때만 사용합니다.
- Mermaid를 사용할 때는 `accTitle`, `accDescr`과 `figcaption`을 작성합니다.
- 공통 스타일은 `assets/style/presentation-theme.css`, 공통 동작은 `assets/js/common.js`를 사용합니다.

## 5. 공통 자산 수정

- `presentation-theme.css`와 `common.js` 변경은 모든 가이드에 영향을 주는 공통 변경으로 취급합니다.
- 하나의 가이드에만 필요한 스타일이나 동작을 근거 없이 공통 자산에 추가하지 않습니다.
- 공통 자산을 수정할 때는 기존 메뉴, 코드 접기, 반응형과 인쇄 동작을 유지합니다.
- JavaScript로 생성하는 UI에는 키보드 사용, Focus 상태와 필요한 ARIA 속성을 함께 제공합니다.

## 6. 검증

- HTML의 Section, Heading, Code Block과 닫는 태그 구조를 확인합니다.
- 내부 링크, CSS와 JavaScript 경로가 실제 파일과 일치하는지 확인합니다.
- 코드 블록 안에 Escape되지 않은 HTML 문자가 없는지 확인합니다.
- `text`가 아닌 모든 코드 블록에 `pre.hljs`와 `hljs-*` 토큰이 있고, 구문 강조 전후의 표시 코드가 같은지 확인합니다.
- HTML의 개별 `TBD` 표시는 `<code class="badge-tbd">TBD</code>` 구조인지 확인합니다.
- `EXAMPLE` 같은 불필요한 예시 라벨이 남아 있지 않은지 확인합니다.
- 각 `blockquote`가 핵심 원칙, 경고, 문서 우선순위 또는 실제 인용처럼 강조할 이유가 있는지 확인합니다.
- Header와 Footer의 문서명이 일치하고 문서 버전, 날짜와 상태 정보가 남아 있지 않은지 확인합니다.
- `common.js`를 수정한 경우 JavaScript 문법 검사를 수행합니다.
- 공통 자산을 수정한 경우 하나의 문서만 확인하지 않고 기존 가이드에 미치는 영향도 검토합니다.
- 가능한 경우 Desktop과 Mobile 너비에서 화면을 확인합니다.
- 시각 검증을 수행하지 못한 경우 완료 결과에 그 제한을 명시합니다.
- 기존 사용자 변경과 관계없는 파일 또는 서식을 함께 수정하지 않습니다.
